$(document).ready(function() {
    $('#menu-servicos-nav').flexVerticalCenter({
        cssAttribute: 'padding-top',
        parentSelector: '#barra-servicos'
    });
    $('#menu-relevancia > li > a').flexVerticalCenter({
        cssAttribute: 'padding-top',
    });
    // $('#menu-principal .sub-menu > li > a').flexVerticalCenter({
    //     cssAttribute: 'padding-top',
    // });
});
